
export const useAlert = () => {

    const setAlertNav = () => {

        window.alert('Me alegra que quieras seguir navegando. Pero de momento esto es un ejercicio de prueba de maquetación de una home page y no existe el destino al que quieres acceder.')
        
    }

  return {
    setAlertNav
  }
}
